# 手工织锦纹样排版台

直接打开`index.html`使用。支持网格绘制、色线统计、撤销重做、本地保存和导出JSON。

## 上机织造卡

- 从当前网格生成横行提综队列：每格对应一个经线通道，一行内连续同色投梭自动合成一段。
- 有色线未处理（底色空格）或画布行列、纹样与卡片不符时，卡片只能留草稿，不能确认。
- 同一通道同一色线连续提综超过四行会在卡上标红，需调整纹样后再确认。
- 卡片确认后画布再改动，旧卡自动作废并留档，可逐条删除或清空留档。

业务拆分：`card.js` 卡片计算、`cardStore.js` 历史存放、`cardPanel.js` 页面操作。
